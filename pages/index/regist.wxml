<!--logs.wxml-->
<view class="index">
    <image class="avatar" src="{{user.wxUserInfo.avatarUrl}}"/>
    <input type="text" bindinput="bindInput" placeholder="请输入注册码" value="{{code}}"/>
    <button bindtap="bindSubmit">提交</button>
</view>
