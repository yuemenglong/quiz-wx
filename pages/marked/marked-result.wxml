<view>
    <text>正确：{{succ}}， 错误：{{fail}}</text>
    <button bindtap="bindAnswer">再来一遍</button>
    <button wx:if="{{fail!=0}}" bindtap="bindRedo">重做错题</button>
    <button bindtap="bindFinish">完成</button>
</view>
