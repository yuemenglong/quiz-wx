<view>
    <text>正确：{{succ}}， 错误：{{fail}}</text>
    <button wx:if="{{fail!=0}}" bindtap="bindReview">浏览错题</button>
    <button wx:if="{{fail!=0}}" bindtap="bindRedo">重做错题</button>
    <button bindtap="bindFinish">结束考试</button>
</view>
